using UnityEngine;

public class Interactions : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Raycast()
    {
        Ray ray = transform.GetComponent<Ray>();
    }
}
